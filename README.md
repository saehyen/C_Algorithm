# 알고리즘 풀이법 및 주의할 점

#### 숫자 문자열과 영단어
- 영단어에 대한 테이블 만들기
- 숫자면 그대로 붙여쓰기
- 영어라면 맞는 숫자 붙여주기
- 문법 : if (strncmp(table[i], s, strlen(table[i])) == 0

#### 문자열 내림차순으로 배치하기
- compare 함수 만들기
- c언어 내부 quick sort가 존재함
- qsort (void *base, size_t nel, size_t width, int (*compare)(const void *, const void *);
- base: 정렬하고자 하는 배열의 포인터
- nel: 배열의 각 원소들의 총 수
- width: 배열에서 원소 하나의 크기입니다.
- *compare: 비교를 수행할 함수 포인터
- 오름차순시 compare함수에서 클 때 1 작을때 -1, 내림차순은 반대로

- 선택정렬도 가능
- 문자마다의 비교 = 파이썬과 같음
- swap함수를 define하고 사용할 것

#### 수박수박수박수박수박수?
- 문자열 끝에 추가하는 함수 : strcat(붙일곳,붙일것);
- 그냥 포인터로 하면 에러 배열로 해야됨 
- 동적할당 이후 strcpy(answer,"")하는 과정 필요.
- 메모리 할당시 글자는 적어도 (sizeof(char)*3+1)만큼 할당해줘야 함
- subak은 sagmentation fault subak2는 성공


#### 소수 만들기
- 변수앞에 형태 선언해주는 것 기억하기

#### 가운데 글자가져오기
- Answer값을 동적할당하여 리턴할것 answer = (char*)malloc(sizeof(char) * 2);
- answer 마지막 값에 '\n' 넣어주기 
- strlen : 문자열 길이

#### 3진법 뒤집기
- 파이썬과는 다르게 c언어에서는 진법을 바꿔주는 라이브러리가 없음
- 배열에 3을 나눈 나머지를 집어넣는 방식으로 해결할 것

#### 2016년
- strcpy를 이용하여 케이스에 맞는 요일을 answer에 복사하는 방식 채택

------------------------------------------------------------------------------

#### 내적 : 어려움 없음

#### 로또의 최고 순위와 최저 순위 : 어려움 없음

#### 음양더하기 : 어려움 없음

#### 두 정수 사이의 합 : 어려움 없음